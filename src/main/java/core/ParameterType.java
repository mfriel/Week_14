package core;

public class ParameterType<COUNTRY, CURRENCY, LOCALE, QUERY, APIKEY, JSON> {
    String COUNTRY,
    CURRENCY,
    LOCALE,
    QUERY,
    APIKEY,
    JSON;

    private ParameterType(){
    }
}
